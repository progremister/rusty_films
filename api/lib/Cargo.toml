[package]
name = "api-lib"
version = "0.1.0"
edition = "2021"

[dependencies]
# actix
actix-web = "4.3.1"

# database
sqlx = { version = "0.7.3", features = ["postgres", "tls-native-tls", "uuid", "chrono", "macros", "json"] }

# tracing
tracing = "0.1.40"
